{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop-base",
  "description": "Core window permissions for bundled local development content only.",
  "windows": ["main"],
  "platforms": ["linux", "macOS", "windows"],
  "permissions": ["core:default"]
}
